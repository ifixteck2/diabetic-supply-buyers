const $ = (id) => document.getElementById(id);
const money = (value) => Number(value || 0).toLocaleString("en-US", { style: "currency", currency: "USD" });
const cleanPhone = (value) => String(value || "").replace(/\D/g, "").slice(-10);
const status = (id, message, type = "ok") => { $(id).innerHTML = message ? `<div class="status ${type}">${message}</div>` : ""; };
let items = [];
let loadedCustomer = null;
init();
async function init() { $("purchaseDate").value = new Date().toISOString().slice(0, 10); bindEvents(); renderItems(); const me = await api("/api/me", { silent: true }); if (me?.ok) showApp(); }
function bindEvents() { $("loginBtn").onclick = login; $("logoutBtn").onclick = logout; $("lookupPhone").addEventListener("blur", lookupCustomer); $("lookupPhone").addEventListener("keydown", (event) => { if (event.key === "Enter") lookupCustomer(); }); $("addItemBtn").onclick = addItem; $("clearItemBtn").onclick = clearItem; $("clearInvoiceBtn").onclick = clearInvoice; $("savePurchaseBtn").onclick = savePurchase; }
async function login() { const result = await api("/api/login", { method: "POST", body: { username: $("username").value.trim(), password: $("password").value, remember: $("remember").checked } }); if (result?.ok) showApp(); else status("loginMsg", result?.error || "Login failed.", "bad"); }
async function logout() { await api("/api/logout", { method: "POST" }); location.reload(); }
function showApp() { $("login").classList.add("hidden"); $("app").classList.remove("hidden"); }
async function lookupCustomer() { const phone = cleanPhone($("lookupPhone").value); if (phone.length !== 10) { status("customerStatus", "Enter a 10 digit phone number.", "bad"); return; } $("lookupPhone").value = phone.replace(/(\d{3})(\d{3})(\d{4})/, "$1-$2-$3"); const result = await api(`/api/customers/lookup?phone=${phone}`); loadedCustomer = result.customer; if (loadedCustomer) { $("customerName").value = loadedCustomer.name || ""; $("customerEmail").value = loadedCustomer.email || ""; $("customerNotes").value = loadedCustomer.notes || ""; status("customerStatus", "Customer found. History loaded."); } else { $("customerName").value = ""; $("customerEmail").value = ""; $("customerNotes").value = ""; status("customerStatus", "New customer. Add their name and save a purchase.", "warn"); } renderHistory(result.invoices || []); }
function addItem() { const item = { category: $("category").value, brand: $("brand").value.trim(), model: $("model").value.trim(), quantity: Number($("quantity").value || 0), expiration: $("expiration").value, condition: $("condition").value, unit_cost: Number($("unitCost").value || 0), expected_sell_each: Number($("expectedSell").value || 0), notes: $("itemNotes").value.trim() }; if (!item.brand && !item.model) return status("saveStatus", "Add a brand or model.", "bad"); if (!item.quantity) return status("saveStatus", "Quantity must be at least 1.", "bad"); items.push(item); clearItem(); renderItems(); }
function clearItem() { $("category").value = "Diabetic Pods"; $("brand").value = ""; $("model").value = ""; $("quantity").value = 1; $("expiration").value = ""; $("condition").value = "Sealed"; $("unitCost").value = ""; $("expectedSell").value = ""; $("itemNotes").value = ""; }
function clearInvoice() { if (!confirm("Clear current invoice items?")) return; items = []; renderItems(); }
function renderItems() { $("itemsBody").innerHTML = items.map((item, index) => `<tr><td>${escapeHtml(item.category)}</td><td>${escapeHtml(item.brand)}</td><td>${escapeHtml(item.model)}</td><td>${item.quantity}</td><td>${escapeHtml(item.expiration)}</td><td>${escapeHtml(item.condition)}</td><td>${money(item.unit_cost)}</td><td>${money(item.quantity * item.unit_cost)}</td><td><button onclick="removeItem(${index})">Remove</button></td></tr>`).join("") || `<tr><td colspan="9">No items added yet.</td></tr>`; $("invoiceTotal").textContent = money(items.reduce((sum, item) => sum + item.quantity * item.unit_cost, 0)); }
window.removeItem = (index) => { items.splice(index, 1); renderItems(); };
async function savePurchase() { const phone = cleanPhone($("lookupPhone").value); if (phone.length !== 10) return status("saveStatus", "Customer phone is required.", "bad"); if (!items.length) return status("saveStatus", "Add at least one item.", "bad"); const result = await api("/api/purchases", { method: "POST", body: { customer: { name: $("customerName").value.trim(), phone, email: $("customerEmail").value.trim(), notes: $("customerNotes").value.trim() }, invoice: { purchase_date: $("purchaseDate").value, payout_method: $("payoutMethod").value, notes: $("invoiceNotes").value.trim() }, items } }); if (result?.ok) { status("saveStatus", `Saved ${result.items_saved} item(s) for this customer.`); items = []; renderItems(); await lookupCustomer(); } else { status("saveStatus", result?.error || "Could not save purchase.", "bad"); } }
function renderHistory(invoices) { if (!invoices.length) { $("history").innerHTML = "<p>No purchase history yet.</p>"; return; } $("history").innerHTML = invoices.map((invoice) => `<article class="history-card"><h3>${new Date(invoice.purchase_date).toLocaleDateString()} - ${money(invoice.total_paid)}</h3><p class="mini">Payout: ${escapeHtml(invoice.payout_method)} ${invoice.notes ? " - " + escapeHtml(invoice.notes) : ""}</p><ul>${invoice.items.map((item) => `<li>${item.quantity}x ${escapeHtml(item.brand)} ${escapeHtml(item.model)} - ${escapeHtml(item.condition)} - ${money(item.unit_cost)} each</li>`).join("")}</ul></article>`).join(""); }
async function api(url, options = {}) { try { const response = await fetch(url, { method: options.method || "GET", headers: options.body ? { "Content-Type": "application/json" } : undefined, body: options.body ? JSON.stringify(options.body) : undefined }); if (response.status === 401 && !options.silent) { $("login").classList.remove("hidden"); $("app").classList.add("hidden"); } const data = await response.json().catch(() => ({})); return response.ok ? data : { error: data.error || "Request failed." }; } catch { return { error: "Could not reach the server." }; } }
function escapeHtml(value) { return String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[char])); }
